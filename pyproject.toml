[project]
name = "etl-llm-finetuning"
version = "0.1.0"
description = "Production-style LLM Fine-Tuning using SFT, LoRA and QLoRA for ETL Pipeline Failure Analysis."
readme = "README.md"
requires-python = ">=3.12,<3.13"

dependencies = [
    "accelerate>=1.14.0",
    "transformers>=5.14.1",
]
